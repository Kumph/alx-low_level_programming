#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point prit base 10 number
 *
 * Return: always 0 (Sucess)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf('\n');
	return (0);
}
