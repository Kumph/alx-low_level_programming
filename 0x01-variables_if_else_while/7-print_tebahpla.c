#include <stdio.h>

/**
 * main - entry point prit base 10 number
 *
 * Return: always 0 (Sucess)
 */
int main(void)
{
	char H;

	for (H = 'z'; H = 'a'; H++)
	{
		putchar(H);
	}
	putchar('\n');
	return (0);
}
