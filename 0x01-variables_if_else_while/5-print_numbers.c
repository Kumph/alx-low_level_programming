#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point prit base 10 number
 *
 * Return: always 0 (Sucess)
 */
int main(void)
{
	char i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
