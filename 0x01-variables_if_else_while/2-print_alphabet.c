#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints alphabet in lowercase
 * Return : Always 0 (sucess)
 */
int main(void)
{
	char M;

	for (M = 'a' ; M <= 'z' ; M++);
	{
		putchar(M);
	}
	return (0);
}
